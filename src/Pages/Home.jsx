import React from 'react';
import Loading from '../components/Loading';

const Home = () => {
    return (
        <div>
            <Loading></Loading> 
        </div>
    );
};

export default Home;